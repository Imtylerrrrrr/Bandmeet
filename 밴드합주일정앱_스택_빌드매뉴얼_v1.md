# 밴드 합주 일정앱 — 스택 & 빌드 매뉴얼 (Claude Code 핸드오프 v1)

> 기획 전문은 `밴드합주일정앱_기획정리_v1.md` 참조. 이 문서는 **스택 결정 + 검증 + 빌드 순서**다.
> 제품: 밴드 동아리 합주·회의 일정 자동 매칭 웹앱. **멀티테넌트 SaaS**(다른 동아리에 확장 + 구독제 목표). MVP는 한 동아리.
> 핵심 난제: 한 사람이 여러 곡 + 합주실 1개 + 이어서 패킹.

---

## 1. 확정 스택

| 레이어 | 기술 | 이유 |
|---|---|---|
| 프론트 + API | **Next.js (App Router, TS)** on **Vercel** | 모바일 우선 웹, 풀스택 JS. 단건 매칭을 TS로 처리 → MVP에 Python 불필요 |
| 스타일 | Tailwind | 드래그 격자·캘린더 등 커스텀 UI |
| DB + 인증 | **Supabase** (Postgres + Auth + RLS) | 멀티테넌트 SaaS를 RLS로 가장 빠르게. 카카오 OAuth 내장. 오픈소스라 자가호스팅 이전 가능(lock-in 낮음) |
| ORM | **Drizzle** | 스키마·마이그레이션 소유 → 데이터 포터블. lock-in 완화의 핵심 |
| 단건 매칭 | TS (Vercel Route Handler) | 평범한 인터벌 로직, 솔버 불필요 |
| 묶음 매칭 (v2) | **Python FastAPI + OR-Tools CP-SAT**, 24h PC(Docker) | 이어서 패킹 = 제약 최적화. JS에 동급 솔버 없음 |
| 카톡 연동 | v1 사설 봇(메신저봇R, 24h PC 에뮬) — 8장 | 추가 채널만. 코어 비의존. 멀티테넌트 시 알림톡 전환 |
| 스케줄러 | **Vercel Cron** | 투표 마감 → 자동 확정 트리거 (필수) |
| 데이터 페칭 | TanStack Query + Server Components | |
| 검증 | Zod | 입력 검증 |

## 2. 배포 토폴로지

```
[모바일 웹 브라우저]
        │
        ▼
[Vercel] Next.js 프론트 + API + Cron + outbox/bot API ──┐ (RLS 보장 경로)
        ▲   ▲                                            ▼
        │   │ (봇이 Vercel로 outbound HTTP만: poll + query) [Supabase] Postgres + Auth + RLS
        │   │
[24h PC] 안드 에뮬 + 메신저봇R (v1 사설 봇)
[24h PC / Docker] CP-SAT 솔버(v2, Cloudflare Tunnel)
```

- **v1:** 24h PC = 카톡 사설 봇(에뮬). 봇은 Vercel로 **나가는 HTTP만** 하므로 PC 외부 노출 불필요(터널 X).
- **v2:** 24h PC에 CP-SAT 솔버 추가 — 이땐 Vercel→PC라 Cloudflare Tunnel 필요.
- Vercel Cron → `/api/cron/close-votes` (매시간) → 마감 투표 집계·확정.

## 3. 데이터 모델 / 스키마

멀티테넌시 = **모든 테이블에 `org_id` + 멤버십 조인**. (한 유저가 여러 동아리 소속 가능)

```
orgs(id, name, plan['free'|'pro'])
profiles(id = auth.users.id, name)                 -- Supabase auth.users 확장
memberships(user_id, org_id, role['운영진'|'부원'])  -- 유저 ↔ org 다대다 + 역할

semesters(id, org_id, name)
performances(id, org_id, semester_id, name, perform_date, deadline, archived_at)
teams(id, org_id, performance_id, name)
team_members(team_id, user_id)

songs(id, org_id, team_id, title, status['후보'|'확정'])
song_members(song_id, user_id)        -- ★ 곡-사람 다대다. 사람 충돌 계산의 핵심

availability_template(user_id, org_id, weekday, hour, tier['green'|'yellow'])   -- 주간 반복
availability_exception(user_id, org_id, date, hour, tier|null)                  -- 날짜별 덮어쓰기

rehearsals(id, org_id, song_id, start_at, duration_min,
           status['제안'|'투표중'|'확정'|'충돌'], is_extra)
  -- status='확정' = 합주실 점유. 한 org 내 '확정' rehearsal은 시간 겹치면 안 됨(방 1개)

rehearsal_votes(id, rehearsal_id, vote_close_at)
rehearsal_vote_options(id, vote_id, slot_start, duration_min)
rehearsal_vote_ballots(option_id, user_id, rank)   -- 랭크 투표(1·2·3순위)

meetings(id, org_id, start_at, ...)   -- 방 점유 X, 사람 충돌만
```

- **방(합주실)**: org당 1개 암묵 → 별도 `rooms` 테이블 불필요. 멀티룸은 v3에서 추가.
- **점유 차감**: 매칭 시 `가용 − (그 유저의 확정 rehearsal + meeting)`.

## 4. 멀티테넌시 & 인증

- **모든 쿼리 org 스코프.** RLS 정책: 사용자가 해당 `org`의 membership을 가질 때만 row 접근.
- **인증**: 카카오 OAuth(Supabase Auth social provider). 역할 가드(운영진/부원)는 membership.role로.
- **⚠ RLS × Drizzle (검증 포인트):** Drizzle은 Postgres에 직결돼서 기본적으로 RLS의 `auth.uid()` 컨텍스트가 안 잡힌다.
  - **권장(솔로 MVP):** Drizzle이 스키마·마이그레이션 소유 + **앱 쿼리는 항상 `org_id` 명시 스코프** + **RLS는 백스톱으로 켜둠**(이중 방어). 사용자 대면 읽기/쓰기 중 RLS 보장이 꼭 필요한 경로는 `@supabase/ssr` 서버 클라이언트 사용 또는 커넥션에 per-request JWT claims(`set local`) 주입.
  - **서비스 롤 키는 서버에서만.** 절대 클라이언트 노출 금지.

## 5. 매칭 엔진

### MVP — 단건 (TS, Vercel)
- 입력: 곡의 `song_members`, 각자 availability(template+exception, tier), 그 유저들의 확정 rehearsal/meeting(점유 차감), 방 점유.
- 알고리즘: **interval-intersection sweep**으로 "≥k명 동시 가능" 계산 → 1~2h 후보창 열거 → 하드 필터(방 빔 + 사람 충돌 없음) → 점수 → top-k 추천.
- **점수 함수:**
  1. 가용 비율 — **lexicographic**: 올-green 슬롯 우선 → 모자라면 green+yellow (yellow 쓴 인원 최소화)
  2. **이어서 보너스** — ITC-2007 compactness 차용: 같은 유저의 인접(연속) 합주에 붙으면 가점 (고립 세션 = 패널티 2). **인접은 시간 경계(끝시각 T = 시작시각 T)로 인코딩, 리스트 인덱스 X**
  3. 방 조각 안 내기 — 기존 점유에 붙는 슬롯 우선
  4. 공연일까지 주차

### v2 — 묶음 / 이어서 하기 (CP-SAT 서비스)
- `POST /solve {songs, members, availability, room_state}` → 패킹된 스케줄.
- 모델: 곡마다 optional interval, **사람마다·방마다 `AddNoOverlap` 하나씩**, **makespan 최소화**(룸 무틈 패킹).
- Vercel → 24h PC 호출 = Cloudflare Tunnel.

### FCFS 단편화 완화 (상한 없이)
- **MRV**: 가능 슬롯 적은 곡 먼저 확정 (tie = 멤버 많이 겹치는 곡 = degree)
- 최소제약 슬롯 선택
- **규모 tiny → 메타휴리스틱 금지.** CP-SAT/완전탐색이면 1초 내 증명된 최적. 과설계가 최대 함정.

## 6. 핵심 워크플로우 / 상태

- **rehearsal 상태머신:** 제안 → 투표중 → 확정 → (변경 감지 시) 충돌 → 재투표.
- **투표 마감:** Vercel Cron(매시간) → 마감 지난 투표 집계 → **최상위 _빈_ 슬롯 자동 확정**(1순위 먹혔으면 2순위로) → 점유 기록 → 알림.
- **충돌 재계산 트리거:** `availability` / `song_members` / `rehearsal` 변경 시 → 영향받는 곡 합주 재검사 → `status='충돌'` 플래그 → **곡 담당자 + 운영진에게 알림**(자동 재배치 X, 사람이 재투표).
- **루프:** 모으기 → 정하기 → 띄우기 → 알림 → (변경 감지)→정하기. 공연 주까지 매주.

## 7. 화면 ↔ 라우트

| 모듈 | route | 핵심 |
|---|---|---|
| 홈(개인 통합 주간) | `/` | |
| 공연 목록(+아카이브) | `/perf` | 🔒운영진 |
| 공연 상세(팀 배정) | `/perf/[id]` | 🔒 |
| 팀 상세 | `/team/[id]` | |
| 곡 후보·투표 | `/team/[id]/songs` | |
| 곡-사람 배치 | `/team/[id]/assign` | |
| 가용성 입력 | `/availability` | ★ 드래그 격자(반복+예외, green/yellow) |
| 합주 매칭·투표 | `/match/[songId]` | ★ 추천·랭크투표 |
| 합주실 마스터 캘린더 | `/calendar` | ★ 주/월/년, 더블부킹 경고 |
| 회의 | `/meeting` | 매칭·캘린더 재사용(방 점유 off) |

★ = 핵심 커스텀 컴포넌트. 나머지는 평범한 CRUD.

## 8. 카톡 연동 (v1 = 사설 봇, 검증 완료)

**결정: v1에서 사설 봇을 24h PC 안드로이드 에뮬(메신저봇R)로 돌린다.** 위험(계정 정지·카톡 업데이트 시 오작동)을 인지하고 채택. **단, 봇은 추가 채널이지 코어가 아니다 — 봇이 죽어도 앱은 100% 작동(앱 직접 조회 + 복붙).**

**구조:**
- **아웃바운드(알림): 폴링.** 앱이 알림을 `outbox` 테이블에 적재 → 봇이 Vercel API를 주기 polling → 메신저봇R `replyRoom`으로 단톡 게시 → 전송 표시. 봇은 outbound HTTP만 → **PC 외부 노출 불필요**.
- **인바운드(조회): 읽기 전용.** `/합주봇 ?월?일` 감지 → 공유 시크릿으로 Vercel 읽기 API 호출 → 일정 텍스트 답장.

**안전 규칙 (필수):**
1. **읽기 + 알림만.** 명령으로 상태 변경(투표/확정) 금지 → 정지 위험↓ + 단톡 메시지 주입 차단.
2. **카카오 부계정**으로 봇 운영 (본계정·동아리 보호).
3. **`outbox`는 멱등** — 봇 크래시에도 중복 게시/누락 없게 "전송됨" 원자적 표시.
4. **단톡방 ↔ org 바인딩** — 운영진이 "이 단톡을 우리 동아리에 연결" 1회 설정 → 봇이 올바른 테넌트 조회.
5. **백엔드 못 닿으면 침묵** — 에러를 단톡에 토해내지 말 것.

**검증된 제약 (배경):**
- 단톡 봇은 카카오 공식 지원 아님 → 메신저봇R류(안드 알림 읽기, 공기계/에뮬 + 부계정)로만. 정지·오작동 위험.
- 오픈빌더 챗봇은 1:1 채널 전용 → 단톡 부적합.
- 알림톡(공식 outbound)은 딜러사 + 사업자 인증 + 정보성 템플릿 심사 필요.

**일관성 플래그:** 사설 봇은 **v1 단일 동아리 단톡 전용.** 멀티테넌트 구독 단계엔 동아리마다 봇 계정·운영이 곱해져 안 맞음 → **유료 확장 아웃바운드는 알림톡으로 전환.** 코어 제품을 사설 봇 위에 짓지 말 것.

## 9. MVP 컷 → v2

**MVP:** 카카오 로그인 + org/멤버십 → 공연/팀/곡(투표)/곡 배치 → 가용성 입력 → **단건 자동추천·랭크투표·마감 자동확정** → 룸 마스터 캘린더(방·사람 충돌 경고) → 개인 통합 뷰 → 소집 복붙/딥링크 + **사설 봇 알림/조회(8장)** → 아카이브.

**v2:** CP-SAT 묶음 매칭(이어서 하기), 회의 일정, 알림톡 전환, 충돌 자동 재계산 정교화, yellow 가중치 튜닝.

## 10. Claude Code 빌드 순서

1. **Scaffold** — Next(App Router, TS) + Tailwind, Supabase 프로젝트, Drizzle 연결, env 정리.
2. **스키마 + RLS** — 3·4장. `org_id`, memberships, 전 테이블 RLS 정책, Drizzle 마이그레이션.
3. **인증** — 카카오 OAuth(Supabase) + 세션 + org 컨텍스트 + 역할 가드(운영진/부원).
4. **구조 CRUD** — 공연/팀/곡/곡 배치 (운영진).
5. **가용성 입력** — 드래그 격자(template+exception, green/yellow). ★ 핵심 컴포넌트.
6. **단건 매칭 엔진(TS)** + 추천·랭크투표 UI (5장).
7. **투표 마감 Cron** → 자동 확정 + 점유 기록 + 충돌 재계산 (6장).
8. **룸 마스터 캘린더 + 개인 통합 뷰** + 더블부킹·사람충돌 경고.
9. **소집 메시지(복붙+딥링크)** + 인앱 알림 + **`outbox` 테이블**.
10. **사설 봇** — 24h PC 안드 에뮬 + 메신저봇R: outbox 폴링 게시 + `/합주봇` 읽기 전용 조회 + 단톡↔org 바인딩 (8장 안전 규칙 준수, 부계정).
11. **아카이브** — 공연 종료 → 읽기전용.

**v2:** CP-SAT 서비스(FastAPI/OR-Tools, 24h PC) + Cloudflare Tunnel, 알림톡 전환, 회의.

## 11. 스택 검증 결과 (함정 체크리스트)

- ⚠ **RLS × Drizzle** — 앱 쿼리에 `org_id` 명시 스코프 + RLS 백스톱. 서비스 롤 키 클라 노출 금지.
- ⚠ **투표 마감엔 스케줄러 필수** — Vercel Cron 없으면 자동 확정이 안 돔.
- ⚠ **카톡 사설 봇 = 추가 채널, 코어 의존 금지** — 봇 죽어도 앱 100% 작동. 읽기+알림만, 부계정, outbox 멱등 (8장).
- ⚠ **솔버는 v2** — Vercel↔24h PC는 Cloudflare Tunnel. MVP는 24h PC 거의 불필요.
- ⚠ **이어서/인접은 시간 경계로 인코딩** — 리스트 인덱스로 짜면 솔버가 구멍 뚫음.
- ✅ **규모 tiny** — CP-SAT/완전탐색이면 충분. 메타휴리스틱 과설계 금지.
- ✅ **존재이유급 리스크 = 가용성 입력률.** 좁은 시간창 + 주간 템플릿 + 리마인더가 생명줄.
- ✅ **데이터 모델 = CB-CTT 정식.** 검증 통과.
