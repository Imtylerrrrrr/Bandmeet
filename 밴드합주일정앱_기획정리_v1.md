# 밴드 동아리 합주·회의 일정 매칭 웹앱 — 기획 정리 (v1)

> 카톡 재투표 지옥 없이, 합주·회의 일정을 자동 매칭하는 웹앱.
> **핵심 난제 = 한 사람이 여러 곡 + 합주실 1개 + 이어서 패킹.** 이 셋이 일반 "노는날 맞추기 앱"과 가르는 지점.

---

## 1. 데이터 모델

```
학기
└ 공연
   └ 팀            (운영진 배정 / 초대)
      └ 곡          (후보 → 투표로 확정)
         └ 곡 담당자  (세션 겹침 → 사람을 곡에 배치)
            └ 곡별 합주 (1~2h, 시간 확정)
```

- **가용성**: 사람별 전역 1개 — `주간 반복 템플릿 + 날짜별 예외`, 칸마다 🟢가능 / 🟡되면가능(조정). 한 번 칠하면 그 사람의 모든 팀·곡·회의 매칭에 공유.
- **합주실 마스터 캘린더**: 점유 1장 = "연별 달력 + 더블부킹 경고 + 합주실 점유"가 합쳐진 단일 객체.
- **역할**: 운영진 / 부원.

## 2. 충돌 2축

| 축 | 정의 | 무엇이 잡나 |
|---|---|---|
| 방 충돌 | 같은 시간 두 합주 | 마스터 캘린더 |
| 사람 충돌 | 한 명이 든 두 곡 합주가 같은 시간 | 곡 담당자 명단 |

- 확정된 합주·회의 → **개인 가용성에서 점유로 차감** (`매칭 입력 = 가용 − 확정된 내 일정`).
- **회의**: 사람 충돌만 체크, 방 충돌 off (비대면 가능, 방 안 먹음).

## 3. 핵심 루프

```
모으기(🟢🟡 칠하기) → 정하기(랭크 투표 → 마감 최다 → 최상위 빈 슬롯)
   → 띄우기(룸 캘린더 + 개인뷰, 점유 차감) → 알림(카톡봇)
   ↺ 변경 감지(가용성·배치·삭제) → 충돌 플래그 → 정하기 재진입
```

공연 주까지 매주 반복. 모든 상태에 나가는 길이 있어 미아(댕글링) 없음.

## 4. 매칭 엔진

**단건 매칭 (MVP):** 열거-점수 방식.
- 시간 격자(평일 17–23 / 주말 11–23, 1h 셀)에서 1~2h 후보 창 전부 열거.
- 하드 필터: 담당자 충분히 가능 + 방 빔 + 담당자 중 확정 합주 충돌 없음.
- 점수 → top-k 추천. (interval-intersection sweep으로 "≥k명 동시 가능" 계산)

**묶음 매칭 / 이어서 하기 (v2):** CP-SAT(OR-Tools).
- 곡마다 optional interval, **사람마다·방마다 `AddNoOverlap` 하나씩**.
- 한 팀의 여러 곡을 하루에 무틈으로 패킹 → **makespan 최소화**.

**점수 요소**
1. 가용 비율 — 🟢 우선 (lexicographic: 올-🟢 풀고 → 안 되면 🟡 fallback)
2. **이어서 보너스** — ITC-2007 compactness: *고립 세션 1개 = 2점* 패널티 그대로 차용
3. 방 조각 안 내기 — 기존 점유에 붙는 슬롯 우선
4. 공연일까지 주차

**FCFS 단편화 완화 (상한 없이)**
- **MRV**: 가능 슬롯 적은 곡 먼저 확정 (tie = 멤버 많이 겹치는 곡 = degree)
- 최소제약 슬롯 선택
- 묶음을 단편화 복구 장치로

**구현 주의**
- 인접은 *리스트 인덱스* 말고 **시간 경계**로 (끝시각 T = 시작시각 T).
- 규모 tiny → **메타휴리스틱 금지.** CP-SAT/완전탐색이면 1초 내 증명된 최적. 과설계가 최대 함정.

## 5. 투표 / 확정

- 가용성 칠하기(🟢/🟡) → 추천 슬롯 **랭크 투표(1·2·3순위)** → **마감시각 최다** → 최상위 *빈* 슬롯 자동 부킹.
- 랭크 투표인 이유: 1순위 슬롯이 다른 곡한테 먹혀도 2순위로 자동 미끄러져 → **재투표 cascade 차단.**
- 룸은 FCFS, 상한 없음.
- 엣지(최선 슬롯에 한 명 빠짐): 앱은 "이 슬롯 3/4, 빠지는 사람 = X"만 까고 → 조정은 카톡 회의. 앱은 immovability 판정 안 함.

## 6. 화면 (10개, 모듈별 분리)

| # | 모듈 | 화면 | |
|---|---|---|---|
| 1 | 홈 | 개인 통합 주간뷰 (내 합주+회의, 충돌 배지) | |
| 2 | 구조 | 공연 목록 (현학기 / 아카이브) | 운영진 |
| 3 | 구조 | 공연 상세 = 팀 배정 | 운영진 |
| 4 | 구조 | 팀 상세 = 곡·멤버 | |
| 5 | 곡 | 곡 후보·투표 | |
| 6 | 곡 | 곡-사람 배치 | |
| 7 | 가용성 | 내 가용성 입력 (반복+예외, 🟢🟡) | ★ |
| 8 | 매칭 | 합주 추천·투표 (단건/묶음) | ★ |
| 9 | 캘린더 | 합주실 마스터 캘린더 (주/월/년) | ★ |
| 10 | 회의 | → 8·9 재사용 (방 점유 off) | |

## 7. 카톡봇

- **조회·알림만.** `/합주봇 ?월?일` 식 조회 + 소집 알림(단톡 복붙 / 딥링크).
- 투표·입력은 앱. (카톡 오픈채팅 봇은 API상 투표 못 돌림)
- 24h PC 폴링 봇으로 충분.

## 8. 운영 / 기타

- 운영진 초대 + 팀 배정(셀프 선택 or 운영진 구조화).
- 지난학기: 운영진 "공연 종료" → 읽기전용 아카이브.
- 공연 직전 추가합주 = "비정기 합주" 플래그 (모델 안 늘림).
- 출석/노쇼 추적 없음.

## 9. 검증 결론 & 리스크

- **모델 = CB-CTT(커리큘럼 기반 강의 시간표) 정식과 일치.** 직관으로 만든 게 교과서와 수렴.
- 🟢/🟡 = lexicographic objective(간호사 로스터링 표준), 이어서 하기 = compactness 패널티 — 둘 다 정립된 기법.
- **유일한 존재이유급 리스크: 부원이 가용성을 안 칠하면 엔진이 굶음** → 카톡으로 회귀. 입력 마찰 최소화(좁은 시간창 + 주간 템플릿 + 봇 리마인더)가 생명줄.
- FCFS 단편화 = 상한 없이도 MRV + 묶음으로 막힘.

## 10. MVP 1차 컷

**MVP (단건만 = CP-SAT 불필요):**
공연·팀·곡(투표)·곡-사람 배치 → 가용성 입력 → **단건 자동추천(열거-점수)·확정** → 룸 캘린더(방·사람 충돌 경고) → 개인 통합 뷰 → 카톡봇 알림 → 아카이브.

**v2:** 묶음 CP-SAT 매칭(이어서 하기), 회의 일정, 추가합주 예외 UX, 🟡 정교화.
